package com.brunobs.audit.configs;

public enum AuditEntityType {
    ACCOUNT,
    ACCOUNT_ENVIRONMENT,
    APPLICATION,
    APPLICATION_ENVIRONMENT,
    ENVIRONMENT,
    SHARING,
    SHARING_PARTICIPANT
}